# Лабораторная работа № 1. Система контроля версий

**Ефимов Денис Борисович, группа 220042-11, вариант 6, лабораторная №1**

Дисциплина «Методы и технологии программирования» (часть 1).

## Задания варианта 6

Средней сложности:
- № 2 — создать локальный репозиторий Python-проекта;
- № 6 — слить ветку `feature` с основной;
- № 8 — создать репозиторий на GitHub и связать его с локальным.

Повышенной сложности:
- № 4 — настроить Git hook для проверки кода flake8 (`pre-commit.sh`, `.flake8`);
- № 10 — реализовать Git flow (`main` / `develop` / `feature/*` / `release/*` / `hotfix/*`, теги `v1.0.0`, `v1.0.1`).

## Запуск

```
python main.py
```

## Установка pre-commit hook

```
cp pre-commit.sh .git/hooks/pre-commit
chmod +x .git/hooks/pre-commit
```
